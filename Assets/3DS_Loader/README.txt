1) Trascina lo script "Loader3DS" in un GameObject vuoto.
2) In "Model Path" inserisci il path del modello e in "Texture Path" inserisci il path della sua texture.
3) Avvia la scena e clicca sul pulsante "Load".
----------------------------------------------------------------------------------------------------------------------
1) Drag the "Loader3DS" script into a empty GameObject.
2) Insert the path model into "Model Path" and insert its texture path into "Texture Path".
3) Start the scene and click the "Load" button.